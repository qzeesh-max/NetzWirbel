# CalculatorHtml

![NetzWirbel Logo](../../logo.png)

This example demonstrates how to build a Calculator application where the UI layout is entirely defined using HTML and CSS, but the logic is wired in C++ using NetzWirbel.
