# MarketDataHtml

![NetzWirbel Logo](../../logo.png)

This example demonstrates a real-time market data grid, but relies on HTML/CSS for its layout instead of the native C++ layout system, while maintaining performance.
